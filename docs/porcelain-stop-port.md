# Implementation prompt: P5 Porcelain Stop

Apply this prompt separately in Neo and the GoDaddy-hosted sister repository. Reference implementation: `Ingwarski/NanoDuck-AI-Consulting`, commit `77062cd`. This prompt is self-contained; no consultation history or design proposal file is needed.

## Task

Implement the approved P5 Stop button in the upper-right of the Discussion header. Its visible height and corner radius must match the consultant avatar at every breakpoint. Preserve functioning cancellation and the current consultation. Work only in the repository in which this prompt is invoked: verify Git root and remote, read its `AGENTS.md` and current SDD documents, and map the reference paths below to actual target modules before editing. Preserve that target's hosting, authentication, storage and provider configuration. Do not copy the reference repository's local-server setup or manifest.

## Exact changes

1. **`public/styles.css`, or the target's equivalent component stylesheet:** replace the old red square Stop face with a horizontal porcelain button. Define shared tokens used by both the consultant avatar and Stop: `--consultant-avatar-size:38px` and `--consultant-avatar-radius:7px`; override size to `32px` at `max-width:700px`. Remove or override competing fixed sizes so the two actually match. Use border-box sizing. The Stop face is 88px wide, its height and min-height use the shared size, and its corner radius uses the shared radius. Use inline-flex, row direction, centered items, `gap:10px`, `padding:0 14px`, and `flex:0 0 auto`.
2. **P5 appearance:** background `linear-gradient(160deg,#fff,#e7e9ef)`; 1px solid white border; text/glyph color `#171a22`; shadow `inset 0 -2px 2px #c4c9d34d,0 3px 9px #0003`; font size 13px, weight 650, line-height 1, letter-spacing -.15px, and no uppercase transformation. The glyph is a filled 12×12px square with 3px corners. Place the visible word `Stop` to its right. Hover background: `linear-gradient(160deg,#fff,#f3f4f8)`. Disabled opacity is .6 with a wait cursor.
3. **Touch and accessibility:** keep the face at the avatar height, but provide a transparent, centered 44px-high hit area using a button `::before` pseudo-element: `content:"";position:absolute;left:0;top:50%;width:100%;height:44px;transform:translateY(-50%);border-radius:inherit`. Give the button `position:relative`. Ensure surrounding overflow does not clip the hit area and that it cannot overlap another control. Retain visible keyboard focus. Under forced colors, use `ButtonText` for the glyph and border and preserve a distinguishable button face.
4. **`public/index.html`, or its equivalent component:** preserve the existing button identity and event binding. The reference uses `id="stop"`, `type="button"`, class `stop-consultation`, `aria-label="Stop consultation"`, and title `Stop consultation`. It contains an aria-hidden `.stop-symbol` span followed by an aria-hidden `Stop` label span. Keep its current hidden/disabled state handling. Do not create a second cancellation button.
5. **Placement and lifecycle:** keep Stop at the upper-right of the Discussion header, separate from New and far from the former Send target. Preserve visibility during long scrolling and while viewing Discussion/Outcome/Sources. Adapt sticky offsets to the target's actual navigation; do not blindly import local pixel offsets. Sending must not autofocus Stop. Active consultation hides the unavailable composer; completion, failure or Stop restores it and preserves the unsent draft and attachments. Keep existing cancellation, process cleanup, stale-response fencing and Retry/Continue behavior unchanged.
6. **`scripts/browser-active-discussion.mjs` and `scripts/browser-check.mjs`, or equivalent browser checks:** replace obsolete square/red assertions and summary wording. Compare rendered Stop height and corner radius directly with a rendered consultant avatar on desktop and at a 390px viewport. Reference expected heights: 38px and 32px; corner radius: 7px; Stop width: 88px. Check the pseudo-element height is 44px. Verify a click 3px above the visible face, within the expanded hit area, cancels an actual synthetic active run. Retain tests for scroll visibility, tab changes, no accidental autofocus, no horizontal overflow, late polling replies and draft/image restoration. Update summaries to say `avatar-sized Porcelain Stop`.

## Documentation and verification

Reconcile current design and behavior statements through the target's installed SDD document owners. Relevant reference documents are `docs/product-idea.md`, `docs/prd.md`, `docs/user-journey.md`, `docs/screen-map.md`, `docs/wireframes.md`, `docs/design-brief.md`, `docs/architecture.md`, `docs/qa-checklist.md`, and `docs/development-plan.md`. Replace current red/square Stop requirements with the scoped P5 correction; preserve historical/frozen visual evidence. Let the SDD orchestrator update the target's own manifest and run its checker. Do not transplant reference hashes, approvals or verification receipts.

Run the target's artifact checks and browser verification. Reference commands are `node scripts/check-artifacts.mjs`, `node scripts/browser-check.mjs --engine=webkit`, and `node scripts/browser-check.mjs --engine=chromium`; use target equivalents if its tooling differs. Inspect desktop/mobile screenshots and test keyboard focus plus forced-colors behavior. Record actual results and distinguish browser-engine automation from physical Safari/iPhone testing.

Inspect the final diff, commit and push to the selected repository's intended branch. Report changed files, commit, checks and any unverified target-specific behavior. Follow that repository's publication workflow; a pushed commit is not deployment proof. Do not restart an active consultation or resubmit its request. For statically served CSS, verify the new stylesheet is served and use a normal page refresh when appropriate.

## Related latest backend correction

If the sister still reports the generic selected-Codex-route failure after a fixed nine-minute wait, also apply the independent **Codex progress deadline correction — 24 September 2026** in [consultation-orchestration-port.md](consultation-orchestration-port.md), reference commit `2edf171`. That section specifies exact provider files, inactivity/absolute deadlines, progress filtering, error mapping and recovery tests. Keep this separate from the P5 presentation change; changing button CSS does not fix provider timeouts.
