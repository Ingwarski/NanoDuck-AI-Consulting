# Prompt for the two hosted sister repositories: show Head's first assignment before research

Use this prompt **independently** in `Ingwarski/NanoDuck-AI-Consulting-Group-Neo` and `Ingwarski/NanoDuck-AI-Consulting-Group`. The local reference is `Ingwarski/NanoDuck-AI-Consulting` at commit `c595090`. Do not edit the local reference or the other sister while working on one target. Verify `git rev-parse --show-toplevel`, `git remote get-url origin`, `git status --short`, and that target's `AGENTS.md` first. Preserve every pre-existing uncommitted change; do not reset or overwrite work already in progress. The Neo checkout contains an independent nested local repository that must remain untouched. The GoDaddy checkout had extensive uncommitted work when this prompt was written; recheck its current state rather than assuming it is clean.

## Failure and desired behavior

In the local reference, a saved consultation spent about nine minutes in `public_research` before **any** recipient-bound Head Consultant assignment was visible. The provider timed out at its 540,000 ms deadline and the coordinator failed the entire run. The requested-output ledger and selected team were already saved, but the owner saw no Head answer. Fix the same owner-visible failure in the selected target: commit a genuine Head assignment before any potentially slow live search; keep the accepted question and confirmed messages durable; a failed/withheld/timed-out search becomes an explicit evidence limitation and must not falsely become a successful fresh check or erase Head's work. A cancellation must still stop the run. Never fabricate a Head assignment, switch models, send private context to the web-enabled invocation, or shorten a provider deadline merely to hide latency.

First establish the **actual** critical path in the target with privacy-safe output-kind start/completion timestamps, outcome category and duration. Do not log prompts, query text, contacts, credentials, private discussions or rejected provider text. Test the first-message order at the persisted-event/API seam, not only the provider call order. A truthful in-progress phase may be shown while Head is still thinking, but it is not a substitute for a real confirmed Head assignment.

## GoDaddy target: exact coordinator change

`src/server/consultation.mjs` currently calls `if (!snapshot.researchAttempted) await researchStep()` immediately before constructing `headTasks`. Move that initial call **after** the complete `headTasks` commit loop and its generation/status check, but **before** constructing/invoking specialist positions. Keep Head's requested-output ledger and AI-selected roster; do not replace Head with a server-authored assignment. Each successful `head_task` must still be appended verbatim and addressed to its selected recipient. On Stop, check `isCurrent()` before starting research and honor the existing abort signal.

Refactor `researchStep` in the same file. For both its non-web `research_query` call and web-enabled `public_research` call: propagate `cancelled`; preserve provider-contract/tool-trace rejection; turn a non-cancellation provider failure or timeout into `{status: "unavailable", reason: <safe provider code>}`. Persist `researchAttempted: true` for the initial attempt and `researchUnavailable: true` plus a precise `researchUnavailableReason` for unsafe or failed attempts. Apply the same limitation semantics to follow-up research without repeating confirmed rounds. An unsafe query is **withheld before** the web-enabled call. Do not claim that a provider outage was an unsafe query: `current()` must give agents a distinct, truthful limitation for each case, instructing them not to invent sources or describe the failed attempt as completed. If existing valid research and a later follow-up failure coexist, identify the failed **attempt** without discarding the earlier source ledger.

Because the first Head task now precedes research, do not attach newly obtained sources to that earlier task. Attach validated returned source metadata to the first specialist position and include the source ledger in every subsequent role context and final synthesis. When resuming a legacy run whose first Head task already contains sources, preserve it and avoid a second attachment. Preserve the encrypted MySQL snapshot, generation fencing, exact selected model/effort, source validation and current authentication/hosting boundaries. Inspect `src/server/run-snapshot.mjs` and `src/server/store.mjs` only if new snapshot fields need schema or recovery handling; do not change storage design just for this fix. The local reference `src/server/consultation.mjs` at `c595090` shows the sequencing and limitation handling, but adapt it to the target's current worktree rather than replacing the file.

## Neo target: different current bottleneck

In `Ingwarski/NanoDuck-AI-Consulting-Group-Neo`, `src/server/consultation.mjs` **already** commits the `headTasks` loop before specialist positions, and `head_task` has `research: false`; there is no initial `researchStep` to move. Do not add a synthetic pre-Head research step or claim the GoDaddy cause applies. Measure accepted-message → optional `auto_team` → first `head_task` provider call → first committed event. Check provider start/end and the existing 540,000 ms deadline in `src/server/codex-provider.mjs`.

The Neo coordinator currently runs `headTaskOutput` with an XML wrapper, imperative/case-anchor/two-sentence/420-character tests; an otherwise successful task may trigger a **second** provider turn, then `headTaskFallback` can replace the result with a generic server-authored task. Remove that semantic rejection, second task-generation call and fallback. Commit one successfully completed, policy-safe Head task **verbatim** to the named specialist; a genuine provider or safety failure leaves a truthful saved failure/retry state. Keep output/tool-trace/secret safety checks and generation fencing. Update the `Head Task` default in `instructions/RUNTIME_PROMPTS.md`, its rendering/migration in `src/server/prompt-contracts.mjs` and any exact saved-public-default upgrade so old accepted instructions do not reintroduce the wrapper; preserve owner-edited guidance and prior revisions. If `auto_team` itself takes the time, retain Head's role-selection authority and exact saved team-size choice, but expose its truthful in-progress state and remove only redundant invocations found by measurement. Do not invent a role roster or task to manufacture a fast first message. The broader Head-led behavior and tests are specified in the local reference `docs/consultation-orchestration-port.md`; reconcile that contract where Neo has not yet received it.

## Focused regressions and completion

Update the target's `test/consultation.test.mjs` and any relevant provider/prompt-contract tests. For GoDaddy, use a deferred `public_research` promise: while unresolved, the run is active and at least one correct recipient-bound Head event is already stored and visible through the normal conversation API; there is no specialist position yet. Resolve it as provider unavailable (the actual 540-second timeout maps to this safe category), then prove the run proceeds, the snapshot records the limitation, later roles see it, the final cannot claim a fresh search, and no System failure is emitted solely for that search. Cover `research_query` failure, unsafe query with zero web calls, Stop during research, Retry without duplicate Head tasks, successful research with more than eight valid sources, and a private contact present elsewhere in the owner request but absent from the web-enabled prompt. Keep all fixtures synthetic.

For Neo, count provider calls: a valid Head task produces exactly **one** `head_task` call and one identical committed assignment, even when long, non-imperative or unusual. A failed provider call produces no generic assignment. Assert the first Head event precedes every web-enabled specialist call; time `auto_team` separately from `head_task`. Test resuming already committed Head tasks without replay, and the complete owner context and assignment received by the selected consultant. Update old wrapper/fallback assertions, not the safety assertions.

In each target, reconcile its own `docs/architecture.md`, `docs/dod-evals.md`, `docs/qa-checklist.md`, `docs/development-plan.md` and `forge/sdd-manifest.json` through the installed `to-sdd-pipeline` owner/checker sequence; amend upstream PRD only if the current target's requirements actually conflict. Do not copy the local reference manifest or local filesystem/TLS assumptions into hosted MySQL applications. Run focused tests, `npm run check`, the available real-browser consultation checks, the SDD audit/implementation check, and inspect the final diff. A live provider search probe is separate from a fixture; report it as untested if not performed. Verify the target's actual deployment process and approval boundary before any hosted publication. Commit/push only changes belonging to the selected repository and report the first-message evidence, test results, deployment state and remaining limitation.

## Later timeout correction — 24 September 2026

The original nine-minute fixed deadline and generic timeout category above describe the earlier incident. For current ports, also apply the detailed **Codex progress deadline correction** in [consultation-orchestration-port.md](consultation-orchestration-port.md): nine-minute inactivity watchdog, thirty-minute absolute ceiling, matching-turn activity filtering, distinct timeout feedback and saved-prefix Retry. Head-first ordering alone does not fix premature termination of a subsequent specialist call.
