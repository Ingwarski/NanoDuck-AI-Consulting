# Deployment boundary

Deployment boundary updated 15 September 2026. The current staged deployment is restricted to the named app's Preview environment.

The authenticated GoDaddy dashboard identifies **Personal AI Consulting Group**, app ID `wy2v0putg6`, published hostname `wy2v0putg6.c35.airoapp.ai`, Preview hostname `wy2v0putg6.preview.c35.airoapp.ai`, Node.js 22 and Europe region. Its GitHub integration now uses `Ingwarski/personal-ai-consulting-web` on `main`. Preview deployed commit `bcf43a5`, reports infrastructure and site status OK.

The user permits erasure of this app and its database to use the replacement, while prohibiting changes to every other GoDaddy app and resource. Preview has created ten `nanoduck_*` tables plus one encrypted owner-instruction record.

The current saved active Codex model settings have been read without exporting conversation content: Head/specialists and Critic both use `gpt-6-astra` / `xhigh`, with a legacy Balanced preset. The inactive Claude branch remains unknown. This historical GoDaddy observation does not define the replacement’s separate specialist-count and discussion-depth controls. Before the eventual reset, verify selected-provider grants can be recovered or reauthorized and use the approved replacement cutover package to target only the nine recorded tables. No executable DROP statement or deletion command is provided because reset is outside this audit.

Other GoDaddy apps, their data, shared/unidentified tables, domains and credentials are outside scope. If attachment or ownership is ambiguous, stop that deletion and ask about the exact ambiguous resource. Do not interpret an app attachment as ownership of everything in a database.

Preview validates Node startup, the owned migration and encrypted bootstrap. Its hosted `NODE_ENV` selected a development sign-in path, so the deployed `bcf43a5` change gives saved `NANODUCK_RUNTIME_MODE=production` precedence. A private Chrome check now reaches the explicit first-use consent gate and an existing private Safari session reaches the workspace, but neither is a fresh Google OAuth completion. Before publishing, validate a fresh Google callback, persistent jobs/storage, streaming or polling, provider access/preflight, a real owner consultation, voice and recovery within this app. GoDaddy injects this app's `DB_*` connection values and exposes encrypted environment secrets rather than a mounted-secret facility: NanoDuck builds the MySQL connection from those values, requires certificate verification, and receives protected Codex `auth.json` as plain or gzip/base64 secret bytes, materializing it only in the temporary owned app-server home for each provider connection. A successful build or health endpoint alone is insufficient.
